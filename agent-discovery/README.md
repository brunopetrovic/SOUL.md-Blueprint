# Agent discovery source manifests

This directory contains canonical source material for website-generated agent discovery surfaces.

The production website generates discovery files from repository state so they cannot silently drift from the current Blueprint version.

Do not advertise an MCP or A2A endpoint as live until it is actually deployed and validated.
