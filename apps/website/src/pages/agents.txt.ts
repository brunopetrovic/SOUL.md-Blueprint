import type { APIRoute } from 'astro';
export const GET: APIRoute=({site})=>{const b=(site||new URL('https://soul-md-blueprint.pages.dev')).toString().replace(/\/$/,'');return new Response(`# SOUL.md Blueprint agent discovery\n\nPurpose: machine-readable access to the SOUL identity engineering standard.\nCanonical source: https://github.com/brunopetrovic/SOUL.md-Blueprint\nllms: ${b}/llms.txt\nfull-corpus: ${b}/llms-full.txt\nmanifest: ${b}/agents.json\npermissions: ${b}/.well-known/agent-permissions.json\nMCP: deploy apps/agent-api and configure its public URL before advertising it as live.\nA2A: deploy apps/agent-api and configure its public URL before advertising it as live.\n`,{headers:{'Content-Type':'text/plain; charset=utf-8'}})};
